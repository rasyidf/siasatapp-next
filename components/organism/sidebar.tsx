import { Button } from '@chakra-ui/button'
import React from 'react'

interface Props {
    
}

const sidebar = (props: Props) => {
    return (
        <>
             <Button variant="unstyled" textColor="white" alignSelf='center' > SIASAT </Button>
        </>
    )
}

export default sidebar